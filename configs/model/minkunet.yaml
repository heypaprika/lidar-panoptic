name: minkunet
cr: 1.0                # channel ratio (0.5 = lighter/faster ablation)
in_channels: 4         # x,y,z,remission
feat_channels: 96      # per-point feature dim into heads
num_classes: 20        # 19 + ignore
