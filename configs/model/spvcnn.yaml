name: spvcnn
cr: 1.0                # channel ratio (0.5 for speed/VRAM ablation)
in_channels: 4         # x,y,z,remission (or 1 = remission only)
feat_channels: 96      # per-point feature dim into heads
num_classes: 20        # 19 + ignore
