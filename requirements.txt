# core
torch>=2.1
numpy
# sparse-conv backbone: spconv (prebuilt CUDA wheel, no source build).
# pick the wheel matching your CUDA — cu120 covers CUDA 12.x (incl. 12.4).
spconv-cu120
# training infra
pytorch-lightning>=2.1
hydra-core>=1.3
wandb
# panoptic clustering / metrics
scikit-learn
# visualization
open3d
matplotlib          # scripts/plot_metrics.py: metrics.csv -> training curves
# panoptic eval reference (or vendor PRBonn/semantic-kitti-api)
# pip install git+https://github.com/PRBonn/semantic-kitti-api.git
